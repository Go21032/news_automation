# ベイカレント時事問題 自動収集タスク

あなたはこのタスクの実行中、以下の手順を**順番に、すべて実行**してください。
これはベイカレント・コンサルティングの選考（論述試験＝時事テーマ2問に賛否の立場を明示して記述する形式）対策として、
日々の時事ニュースを収集し、想定回答例を作成し、Googleスプレッドシートに記録するCIジョブです。

## 手順1: 4軸×指定サイトを調べる

以下の表の軸ごとに、指定サイトを中心にWebSearch/WebFetchツールで
**直近24時間以内**（なければ直近3日以内）の記事を調べ、
出題可能性が高いと判断した記事を**各軸最大5本、合計最大20本**選んでください。
1軸につき5本に満たない場合は、無理に埋めず見つかった分だけでよい。

| 軸 | サイト | URL |
|---|---|---|
| ①AI・技術×雇用 | ITmediaビジネスオンライン | https://www.itmedia.co.jp/business/ |
| ②DEI・ジェンダー・多様性 | 東洋経済オンライン（無料記事のみ） | https://toyokeizai.net/ |
| ③企業経営の意思決定（M&A・リストラ） | Reuters日本語版 | https://jp.reuters.com/ |
| ④社会保障・マクロ経済・政治 | NHK NEWS WEB | https://www3.nhk.or.jp/news/ |

### 選定基準
- 「賛否が分かれる」「ステークホルダー間でトレードオフがある」テーマを優先する。
- ベイカレント過去問と同系統のもの（生成AIと雇用、DEI/トランプ声明、女性管理職比率、LGBT、
  大量解雇、そごう・西武売却、年金と自助努力、#KuToo 等）を優先する。
- 同じ軸内で5本選ぶ際は、同一テーマの類似記事ばかりにせず、できるだけ異なる論点・企業・
  サブテーマの記事を選び、多様性を持たせること。
- 該当記事が5本に満たない軸・1本もない軸があっても無理に埋めず、見つかった分だけでよい。
- **実在するURLのみ**使用すること。推測や記憶だけで記事をでっち上げてはならない。
  リンク切れ・有料会員限定で本文が読めない記事は除外する。

## 手順2: 軸ごとに1本だけ選び、回答例を作成する

**回答例(立場・理由等)を作るのは、軸ごとに集めた最大5本の中から
「最も出題可能性が高い・最も注目度が高い」と判断した1本のみ**とする。
残りの記事は見出し一覧として記録するだけでよく、回答例は作らない
(コストと処理時間を抑えるため、回答文生成は各軸1本・合計最大4本までに限定する)。

選定基準(手順1の選定基準に加えて):
- 話題性・時事性が高く、直近で最もニュースになっている記事を優先する。
- ベイカレント過去問と最も近いテーマ・構造の記事を優先する。
- 賛否の論点が明確で、回答例が書きやすい(具体的な利害対立がある)記事を優先する。

選ばれた1本については、以下のテンプレートに沿って回答例を作成する。

- 立場：賛成 / 反対
- 理由1（経済・経営合理性の観点）
- 理由2（人・社会への影響の観点）
- 想定反論 → 返し
- コンサルならどう提案するか（一言）

回答は「結論→理由2つ→反論への応答→条件付き結論」の型を意識し、
断定的な理念論ではなく、企業経営・組織運営の視点を混ぜること。

選ばれた1本は、`見出し` の先頭に目印として `⭐` を付ける
（例: `⭐生成AI導入で「能率低下」と回答した企業が...`）。
選ばれなかった記事の `見出し` には `⭐` を付けない。

## 手順3: JSON形式で保存する

手順1・2の結果を、リポジトリ直下に `output.json` として保存してください。
スキーマは以下の配列形式に**厳密に**従うこと（キー名を変更しない）。

```json
[
  {
    "軸": "①AI・技術×雇用",
    "見出し": "⭐生成AI導入で「能率低下」と回答した企業が...",
    "url": "https://...",
    "媒体": "ITmediaビジネスオンライン",
    "要約": "1〜2文の要約",
    "立場": "賛成",
    "理由1": "...",
    "理由2": "...",
    "反論と返し": "...",
    "コンサル視点": "..."
  },
  {
    "軸": "①AI・技術×雇用",
    "見出し": "回答例を作らなかった記事の見出し",
    "url": "https://...",
    "媒体": "ITmediaビジネスオンライン",
    "要約": "1〜2文の要約",
    "立場": "",
    "理由1": "",
    "理由2": "",
    "反論と返し": "",
    "コンサル視点": ""
  }
]
```

- 回答例を作らなかった記事は、`立場`〜`コンサル視点` を空文字列 `""` にすること。
- 各軸の中で `⭐` が付くレコードは**必ず1件だけ**にすること(0件・複数件は不可)。
- 該当記事が1つもなかった場合は空配列 `[]` を保存すること。

## 手順4: スプレッドシートへ書き込む

Bashツールで以下を実行し、`output.json` の内容をスプレッドシートに反映させてください。

```bash
python scripts/write_to_sheet.py output.json
```

このスクリプトは、実行日（JST）の日付名でシートを作成（既存なら再利用）し、
見出しの重複を避けながら行を追記します。書き込みロジック自体は変更しないでください。

## 手順5: 完了報告

最後に、以下を簡潔に出力してタスクを終了してください。
- 収集できた記事数（軸ごと、うち回答例を作成した⭐1本を明記）
- 「該当なし」だった軸があればその旨
- スプレッドシートへの書き込み結果（write_to_sheet.pyの出力をそのまま転記でよい）
